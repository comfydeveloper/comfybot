<Window x:Class="ComfyBot.Application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowState="Maximized"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Resources>
            <Style x:Key="Tag" TargetType="Border">
                <Setter Property="Margin" Value="2 1" />
                <Setter Property="Padding" Value="3" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="1" />
                <Setter Property="BorderBrush" Value="#9E9E9E" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition MinHeight="100" Height="100"/>
        </Grid.RowDefinitions>
        <TabControl>
            <TabItem Header="Responses" DataContext="{Binding Tabs.Responses}" IsSelected="{Binding IsSelected}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Button Command="{Binding AddResponseCommand}" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Plus" />
                        </Button>
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Responses}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Background="White" Margin="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200" />
                                            <ColumnDefinition Width="2" />
                                            <ColumnDefinition Width="400" />
                                            <ColumnDefinition Width="2" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <!-- Users -->
                                        <StackPanel Grid.Column="0" Margin="2">
                                            <DockPanel LastChildFill="False">
                                                <Label Content="This person" FontWeight="Bold" />
                                                <Border VerticalAlignment="Center" Margin="2 0" Cursor="Hand" Background="Transparent">
                                                    <Border.InputBindings>
                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding AddUserCommand}"/>
                                                    </Border.InputBindings>
                                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                                                </Border>
                                            </DockPanel>
                                            <ItemsControl ItemsSource="{Binding Users}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Style="{StaticResource Tag}">
                                                            <DockPanel LastChildFill="False">
                                                                <TextBox Text="{Binding Text}" MinWidth="20" BorderThickness="0" />
                                                                <Border Background="Transparent" Cursor="Hand">
                                                                    <Border.InputBindings>
                                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.RemoveUserCommand}" CommandParameter="{Binding}"/>
                                                                    </Border.InputBindings>
                                                                    <materialDesign:PackIcon Kind="WindowClose" VerticalAlignment="Center"  />
                                                                </Border>
                                                            </DockPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0 5" />
                                        <!-- Keywords -->
                                        <StackPanel Grid.Column="2" Margin="2">
                                            <!-- Loose Keywords -->
                                            <DockPanel LastChildFill="False">
                                                <Label Content="Says any of these" />
                                                <Border VerticalAlignment="Center" Margin="2 0" Cursor="Hand" Background="Transparent">
                                                    <Border.InputBindings>
                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding AddLooseKeywordCommand}"/>
                                                    </Border.InputBindings>
                                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                                                </Border>
                                            </DockPanel>
                                            <ItemsControl ItemsSource="{Binding LooseKeywords}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Style="{StaticResource Tag}">
                                                            <DockPanel LastChildFill="False">
                                                                <TextBox Text="{Binding Text}" MinWidth="20" BorderThickness="0" />
                                                                <Border Background="Transparent" Cursor="Hand">
                                                                    <Border.InputBindings>
                                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.RemoveLooseKeywordCommand}" CommandParameter="{Binding}"/>
                                                                    </Border.InputBindings>
                                                                    <materialDesign:PackIcon Kind="WindowClose" VerticalAlignment="Center"  />
                                                                </Border>
                                                            </DockPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <!-- All keywords -->
                                            <Label Content="Says all of these" />
                                            <!-- Exact keywords --> 
                                            <Label Content="Says exactly one of these"/>
                                        </StackPanel>
                                        <GridSplitter Grid.Column="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0 5"/>
                                        <StackPanel Grid.Column="4" Margin="2">
                                            <DockPanel LastChildFill="False">
                                                <Label Content="And I reply every "/>
                                                <TextBox Width="30" Text="{Binding Timeout}" />
                                                <Label Content="seconds with" />
                                            </DockPanel>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Shoutout" DataContext="{Binding Tabs.Shoutouts}" IsSelected="{Binding IsSelected}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding AddShoutoutCommand}" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Plus" />
                        </Button>
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible">
                        <ItemsControl ItemsSource="{Binding Shoutouts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0 2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="100" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="30" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" Content="Command" VerticalAlignment="Center" />
                                        <TextBox Grid.Column="1" Text="{Binding Command}"  />
                                        <Label Grid.Column="2" Content="Message" VerticalAlignment="Center" />
                                        <TextBox Grid.Column="3"  Text="{Binding Message}" />
                                        <Border Grid.Column="4" VerticalAlignment="Center" Margin="2 0" Cursor="Hand" Background="Transparent">
                                            <Border.InputBindings>
                                                <MouseBinding MouseAction="LeftClick" Command="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.RemoveShoutoutCommand}" CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                            <materialDesign:PackIcon Kind="WindowClose" />
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <materialDesign:PackIcon Kind="Cog"></materialDesign:PackIcon>
                </TabItem.Header>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" />
        <!-- Console -->
        <TextBox Grid.Row="2" 
                 x:Name="ConsoleTextBox" 
                 TextWrapping="Wrap" 
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Visible" />
    </Grid>
</Window>
